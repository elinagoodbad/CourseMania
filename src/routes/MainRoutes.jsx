import React from "react";
import { Route, Routes } from "react-router-dom";

const MainRoutes = () => {
  return (
    <Routes>
      <Route path="" element={""} />
    </Routes>
  );
};

export default MainRoutes;
